export enum CONTENT_STATUS {
  DRAFT = 'draft',
  REVIEW = 'review',
  PUBLISHED = 'published',
  DELETED = 'deleted',
  PROCESSING = 'processing',
}

export enum GROUP_CRAWL_CONTENT_STATUS {
  CRAWLED = 'crawled',
  PROCESSING = 'processing',
  PUBLISHED = 'published',
  DELETED = 'deleted',
}
